---
title: Checkbox
---

A `Checkbox` is a graphical user interface element that allows users to select an option. It displays a small square that can be checked or unchecked.

import { Code } from '@astrojs/starlight/components'
import { Tabs, TabItem } from '@astrojs/starlight/components'
import ApiProperties from '@/components/Checkbox/apis/apiProperties.vue'
import ApiSlots from '@/components/Checkbox/apis/apiSlots.vue'

import SCheckboxVariant from '@/components/Checkbox/variant.vue'
import SCheckboxVariantRaw from '@/components/Checkbox/variant.vue?raw'

import SCheckboxSize from '@/components/Checkbox/size.vue'
import SCheckboxSizeRaw from '@/components/Checkbox/size.vue?raw'

import SCheckboxDisabled from '@/components/Checkbox/disabled.vue'
import SCheckboxDisabledRaw from '@/components/Checkbox/disabled.vue?raw'

## Component Usage

### Variant

Use the `variant` prop to control the visual style of the checkbox.

<SCheckboxVariant client:only="vue" />
<Code code={SCheckboxVariantRaw} lang="vue" title="variant.vue" />

### Size

Use the `size` prop to control the size of the checkbox.

<SCheckboxSize client:only="vue" />
<Code code={SCheckboxSizeRaw} lang="vue" title="size.vue" />

### Disabled

A disabled `Checkbox` is a non-interactive form element, meaning users can't check or uncheck it. It's visually distinct, often dimmed, to signal that it's not available for selection.

<SCheckboxDisabled client:only="vue" />
<Code code={SCheckboxDisabledRaw} lang="vue" title="disabled.vue" />

## API

<Tabs>
	<TabItem label="Properties">
		<ApiProperties client:only="vue" />
	</TabItem>
	<TabItem label="Slots">
		<ApiSlots client:only="vue" />
	</TabItem>
</Tabs>
